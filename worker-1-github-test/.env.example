# Copy this file to `.env` and fill in real values. `.env` is git-ignored.
#   cp .env.example .env

# GitHub personal access token used by the worker (needs repo access).
# Create one at https://github.com/settings/tokens
GITHUB_TOKEN=

# Target repository in "owner/name" form, e.g. octocat/hello-world
GITHUB_REPO=
